# W1 预登记：盯盘信号改为「相对恒指的短时超涨超跌取反」

登记时间：2026-09-24，**在打开验证集之前**。本文件写好后不再修改；结果写进 `reports/W1_RESULTS_CN.md`。

## 数据

- 140 只港股通 1m（OpenD `request_history_kline`，QFQ，全部在 30 天已扣费清单内，未消耗新额度），
  2026-06-01 → 2026-09-21，79 个交易日、10,994 个标的-日；另补 2026-09-22、09-23 两天（92,680 行）。
- 恒指 `HK.800000` 1m 同期（同样免费）。
- **训练集**：2026-06-01 → 2026-08-14（54 天）。**验证集**：2026-08-17 → 2026-09-21（25 天）。
  **新样本**：2026-09-22 → 09-23（2 天，只报告，不作门槛）。
- 验证集只评测一次，不调参。

## 训练集上的探索（已做，如实记录）

- 现有信号（VWAP/EMA/RSI 顺势 + BOS/FVG/SWEEP）：15.7 个/标的-日，15m 均值 −4.4bp，按日聚类 t = −10.9。
- BOS、FVG 触发**顺势方向为负**（下一根入场 30m：−2.4bp、−1.4bp）；SWEEP ≈ 0。
- 最强单变量：相对大盘的 5 分钟走势（按 ATR 标准化）取反。
- 约 40% 的表面优势是买卖价差反弹：所以**主口径改为下一根收盘入场**（`d1f30`：第 i+1 根收盘买、第 i+31 根收盘平）。
- 试过无增益、不采用：下一根反转确认（优势消失）、趋势日对齐拆分、恒指同向过滤、预热缩到 16 根、岭回归合成分（与简单规则持平）。
- 大盘口径：140 只等权 +5.71bp，恒指 +5.27bp，恒生科技 +5.15bp，盈富 +5.09bp。实盘用恒指（只多一个订阅）。

## 定义

- `zrel5 = (close/close[-5] − hsi/hsi[-5]) / (ATR14 / close)`，`zrel15` 同理。
- 信号：`zrel5 < −k` 且 `RSI14 < 100−r` → BUY；`zrel5 > k` 且 `RSI14 > r` → SELL。
- 预热 25 根、同方向 15 根内不重复，与现状一致。

## 候选（5 个）

| id | 规则 | 训练集 个/标的-日 | d1f30 均值 | 命中 | t_day |
|---|---|---|---|---|---|
| C1 | k=5, r=65 | 2.79 | +5.27bp | 57.2% | 8.6 |
| C2 | k=6, r=60 | 2.85 | +5.14bp | 56.7% | 10.1 |
| C3 | k=8, r=65 | 1.34 | +5.57bp | 57.7% | 8.5 |
| C4 | k=5 或 zrel15>12, r=65 | 2.87 | +5.22bp | 57.2% | 8.3 |
| C5 | k=4, r=60 | 5.51 | +3.95bp | 55.8% | 7.8 |

命中 = 非零收益中方向对的比例。

## 选择规则（固定）

训练集上，满足 密度 ≥ 1.0 个/标的-日、d1f30 均值 ≥ 4bp、命中 ≥ 55% 的候选中，取 d1f30 按日聚类 t 最大者
（t 同时奖励数量与质量）。另做留一天交叉验证：每次去掉一个训练日、在其余日上按同一规则选，记录被留出那天的结果，报告样本外估计。

强档（看板加粗大写）固定为 `k=8, r=70` 的子集，只作显示，不参与选择。

## 验证门槛（全部满足才替换盯盘默认信号）

1. 验证集 d1f30 均值 > 0 且按日聚类 t ≥ 3；
2. 验证集方向命中 ≥ 53%；
3. 验证集 BUY、SELL 两侧 d1f30 均值都 > 0；
4. 验证集上优于现有信号（同口径 d1f30）。

另报告但不作门槛：新样本两天；4 个常盯标的（02513 / 09988 / 01810 / 00100）；15m 与至收盘口径；强档。

成本提醒：港股往返成本（价差 + 印花税等）远高于这里的毛收益，**这仍是盯盘方向参考，不是可盈利策略的证明**。
