# W9 预登记：价位反应信号按「持有到收盘」检验，并在全新数据上复核 W6

登记时间：2026-09-24，W7 / W8 之后；**2025-06 → 2025-11 的数据下载之前**。写好后不改。

## 为什么要新数据

W7 的主口径（30 分钟）失败，但同一批信号「持有到收盘」在选择段（R5 +5.30bp t=2.3、R4 +7.21bp t=2.1）和验证段都为正。
改口径是看过结果之后的决定，而 08-17 → 09-23 的验证段已经被看过，不能再用来确认。所以用一段**从未下载过**的数据做验证。

## 数据

- 选择段：2025-12-01 → 2026-08-14（与 W6–W8 相同）。
- **新验证段：2025-06-02 → 2025-11-28**，同 140 只港股通 1m（QFQ），已扣费标的免费重拉；日 K 已有（2024-06 起）。
  下载完成前不看任何结果；验证段只评测一次。

## 候选（3 个）

价位与信号定义完全同 W7（PDH/PDL、09:31–09:45 开盘区间；影线拒绝与失败突破；同方向 15 根去重）。

| id | 信号 |
|---|---|
| V1 | W7 的 R5（R1–R4 任一） |
| V2 | W7 的 R4（开盘区间失败突破） |
| V3 | R5 且方向与近 5 日涨跌相反（`s × ret5d ≤ 0`，即 W6 的日线背景同向） |

## 口径与选择

主口径 `fclose`（下一根收盘入场、持有到收盘），次口径 `f30`。多空对称 `bal`，t 按交易日聚类。主评估：价位细（tpb ≥ 2）。
选择段上取 `bal fclose` t 最大者。

## 验证门槛（新验证段，价位细，全部满足才加进盯盘）

1. `bal fclose` > 0 且 t ≥ 2；
2. BUY、SELL 两侧 `fclose` 均值都 > 0；
3. `bal fclose` 高于同段现有 `marks()` 标注的 `bal fclose`。

## 顺带复核 W6（不改变 W6 已有结论，只报告）

在新验证段上重算 W6 的 K5：`D+` 与 `D-` 两组的 `bal fclose`、差值 t、两侧分拆。
若方向相反且显著（差值 t ≤ −2），在报告里明确写出，并建议撤下 `D+`/`D-`。
