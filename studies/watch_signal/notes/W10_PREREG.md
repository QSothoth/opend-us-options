# W10 预登记（只报告）：在 2025-06 → 2025-11 新数据上复核其余 W6 候选，并评估组合方案

登记时间：2026-09-24，W9 之后、**2025H2 数据下载完成之前**。写好后不改。全部只报告，不作门槛、不改变任何已有结论。

1. W6 的 K1–K4 在新数据上的「支持 − 不支持」差值与 t（价位细，持有到收盘），看 K5 是否只是其中碰巧最好的一个。
2. 组合方案「只看 `D+` 标注 + W9 的 V3 信号」相对「全部现有标注」的 `bal fclose`、`bal f30`、密度（价位细）。
3. `D+` 在 30 分钟口径上是否也为正（W6 验证段为 +2.27bp），用来决定盯盘说明里能不能说「短线也有用」。
