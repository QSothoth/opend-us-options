# 数据隔离修复后的固定验证复测

冻结记录：`../results/FROZEN_ISOLATION.json`。两个策略先完成全部 124 个训练 case 的回放核对，未改变参数，然后使用原 `custody-eval-2026-09-14` 的 3 个 case 复测。所有逐笔记录及汇总与修复前完全一致。

| 方案 | 完成 | 净期权盈亏 | 净美元盈亏比 |
|---|---:|---:|---:|
| 进取型 | 3/3 | -$61.84 | 无盈利样本 |
| 稳健型 | 3/3 | +$87.49 | 1.65 |

每 case 一张指定期权，扣单边 $0.65 手续费及 25 bps 滑点。使用原 OHLCV 延迟成交模型，不是真实成交。固定时刻基准此前净盈利 +$415.96，仍优于这两个候选。验证结果不用于调参或候选排名；3 个 case 是已知固定验证集，不是新的盲测。全程 dryrun，无券商订单。
