# Scratch / one-off helpers

These scripts may open `OpenSecTradeContext`. They are **not** part of the
read-only options data pipeline and must not be wired into lowbuy production
cron or quant_flow.
